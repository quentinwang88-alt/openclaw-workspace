# 自动供稿修复后复核：b3404fd

审查范围：`7b878c1..b3404fd`，对照 `OPV_CONSUMPTION_FIX_EXECUTION_PLAN_20260917.md`。本轮未改业务代码、未采集/生图/发布；仅只读代码、合同和本地样片，并使用临时库、假模型、假飞书做隔离验证。

结论：上一轮若干故障已真正修复，但 A/B/C 尚不能整体通过验收。最主要问题仍是跨模块参数和数据结构没有完整接通。建议保留已完成修复，完成以下断点后再进入自动发布阶段。

## 1. 已确认有效的修复

- 旧 `search_to_library.import_feeds` 的 args 异常已修，新旧入口共享函数。
- CALL_CAP=1、两 slot 的隔离探针现在只有一次模型调用、一条新行。
- 同日建行成功、attach_record 失败，下轮可按 slot 补绑，不重复建行。
- “素材生成 1/4”一篇＋“生成中”三篇，现在统计为四篇。
- target_inventory 可穿过 supply_policy 白名单。
- 非旅行配色样例 D1 已进入通用预设，不再默认声明15–22°C。
- 查询族按 query_hits 归属、用途库存及真多搭/可适配拆分已有实现。
- 新版本未进一步全库重析；v3 仍为188篇。

本轮运行8个相关测试文件：181 passed，7 subtests passed，15.24秒。未重跑开发报告所列完整1522项测试。下列失败来自另外的边界/跨模块探针，说明当前绿测不能替代方案验收。

## 2. 必须先处理的发现

### F1 [P1] 目的地查询导出了，但采集器没有读取

位置：OPV `scripts/export_material_gaps.py:225–237`；lab `scripts/collect_by_demand.py:81–88`。

带日本等目的地的需求被放入 `ledger_demands`；采集器只遍历 `demands`，且要求每项有 family，前者没有统一提供该字段。

无网络集成探针：使用真实 collector.main、临时库，输入 demands=[]、ledger_demands=[日本短外套查询]，mock search 调用次数为0，脚本正常退出。实际 query_hits 仍只有上一轮11个通用搜索词，没有本轮日本查询记录。不能用“生成出了日本背景”证明目的地采集已接通。

最小修复：统一 exporter/collector 的需求合同，至少有 demand_key、family、queries、具体需求快照；整条 exporter→collector→query_hits 做一个无网络集成测试。随后小批真实采集验证，不扩量。

同时保留两个相关限制：当前需求固定写三用途、environment_inspiration，丢了 city；终选失败没有记录新 demand。按查询族计算可用库存也未真正判断该具体商品/目的地需求，需求满足后没有关闭/冷却。修复时明确“仅穿搭无需同城”和“环境借鉴按地点判断”，避免把所有旅行账号都变成必须寻找当地照片。

### F2 [P1] brief 前置只移动了变量，终选仍未收到账号定位、视觉和目的地

位置：`services/auto_photo_supply.py:603–626、715–719`；`services/material_adapter.py:404`。

effective_brief 提前构造，但 select_reference 仍只接 theme/product/temperature_band。隔离探针为定位、视觉预设、地点填三个唯一字符串，实际终选 prompt 三者均不存在。

这影响矩阵账号选材，不能归为“后续可选优化”。下游能把图画成日本，并不代表选材已按账号主张和目的地决定参考用途。

最小修复：将同一 brief 显式传入终选与相关匹配，测试实际 prompt 和最终合同，不能只断言数据库存在该列。

B2 仍需收敛的分支：

- `:774` 参考优先仍无条件继承 default_theme；默认主题不再只是偏好。
- `:785–788` 参考原题/选择理由只要含旅行词，就自动赋凉爽旅行与温度。
- `:791–795` 非旅行切通用时直接 band=None，可能清除本篇显式温度；通用预设硬编码TH，扩到其他市场/类目需受适用范围限制。
- `_generalize_reference_topic` 只替换数量，不完成商品适配。D2合同 topic_statement 仍写“灰色连帽拉链卫衣”，虽然最终图片和文案由商品锚点纠正为浅蓝外套。不要据此称合同主张已适配。

无需新增大量主题，只修权威需求→选材→执行结构的现有边界。

### F3 [P1] 参考用途参数没有传到规划图片层，模型分析仍可扩大借鉴范围

位置：`services/feishu_workflow.py:1906`、`photo_reference_vision.py:924–927、1049–1052`。

workflow 传入 untagged_uses，但 plan_travel_content 调用 _travel_planning_images 时遗漏该参数。隔离 spy：调用外层传 ['OUTFIT']，内层收到 MISSING。

即使补传，当前逻辑也优先使用视觉分析返回的 reference_uses；大多数分析已有用途，所以合同限制仍可能被模型自动用途覆盖。D2/D3规划文件可见 OUTFIT+VISUAL_STYLE 或三用途全部存在，三条验收样例均为 overall，未验证仅搭配/仅视觉边界。

最小修复：由冻结合同为所选页生成实际用途，分析用于补充内容而非扩大权限；该用途同时进入规划、生成和QA。需要测试“合同仅搭配、模型分析返回环境+风格”时仍只借搭配。不要仅修一个未标注默认参数就宣称完成。

用途校验函数还有语义空洞：`material_adapter.py:378–392` 不检查 narrative 是否可用；某用途不可用可自动切换成另一用途，却没有确认是否仍满足本篇要求。确定性删除无关维度可以，不能为通行而静默改变必要采用方式。

### F4 [P1] 响应未知＋列表延迟仍重复建同一slot，跨日恢复仍遗漏

位置：`services/auto_photo_supply.py:913–920、472–475、296–301`。

空创建响应仅返回 pending_reconcile，没有持久化已提交未知状态；slot释放后，下轮暂查不到行就再次创建。

临时库＋假飞书探针：第一次远程成功但返回[]，第二次列表暂不可见，最终remote_rows=2，两条来源标记完全相同。

另外，扫描只将 self.today 的标记加入待恢复集合；前一天 attach 失败的旧行第二天仍无合同，又可新增当天任务。

最小修复：持久化 submitting/unknown；恢复查询跨日期未完成合同，用标记实际日期定位；查无结果不能立即重新提交。不需要人工审批，但需要下一轮继续对账。

### F5 [P2] 未知库存仍被当零；分析图片额度未成为逐调用限制

库存位置：`auto_photo_supply.py:305–307、315–335、383`。未知进度返回 known=False，但扫描器直接忽略，没有将账号设为库存未知。

现有workflow实际状态“素材重拍 1/4”、篇数4的隔离探针，仍新增一条自动任务。最小修复是传递 unknown 并暂停该账号新增，结合已有机器状态逐步收敛；不要仅不断扩大字符串白名单。

图片额度位置：`material_analysis.py:821–839`。调用数已预留，但len(batch)没有预留。IMAGE_CAP=1，分析两图仍调用模型、日志images=2。只在供稿启动快照查图片数，无法限制独立分析入口。

修复为同一事务预留调用数和分析输入图数量；继续区分分析图片额度与生图额度。

## 3. 样片：部分效果更好，D1仍不宜判完整通过

查看 `/tmp/dsamples` 中D1封面、第2页、第5页，D2封面和D3封面；未逐张审完三组所有页面，未重新做泰文全文校对。

- D1 `recvvtk8K2ApPL`：确实形成配色方向，纯色背景统一；但四宫格封面大标题穿过左上人物脸部，小标也压在额头。内页“明亮丹宁”“经典酒红”等命名没有解释封面承诺的三种配色公式。三种公式可对应四套造型，不认为数字本身必然矛盾；问题是关系没有表达。
- D2 `recvvtkPnS6cJI`：所看封面浅蓝商品与问题相符；不能仅凭封面断言四页商品细节全部一致。合同仍残留参考灰色卫衣是实现问题，成片碰巧正确不应掩盖。
- D3 `recvvtkhmCYHD0`：封面场景具有日式街道特征，未见明显目的地冲突；标题仍是通用凉爽城市穿搭。可以证明目的地传到了生图，不能证明目的地搜索/选材闭环已通过。

样片最小修复：只对新通用四宫格设置真实标题留白，优先用既有底图重排；内页从已规划配色关系生成一句解释，而非只贴造型名。不重做全部模板，不增加新人工审核门。

## 4. 其他尚未完成项

- 零候选先补分析已经移动到前面，但正式 `scripts/run_auto_photo_supply.py:107–114` 仍未传 analyzer，正常入口不会进入该补分析分支；即使注入，当前 analyze_pending 仍全库前5，而非当前需求范围。首轮补成1–2候选后还能再补5，一轮可能两次。
- 缓存仍按note_id排除已分析，标题/正文及当前指纹更新未完整处理。不要因此再全库重析。
- 自动发布授权、生成provider/model/request_id落盘、精确子项/revision库存仍在开发报告的剩余项，不把它们当作已经交付。

## 5. 下一步建议

集中修复F1–F4的接口贯通与未知响应保护，同时顺修F5和D1排版。验收改为实际跨模块测试：

1. 唯一定位/视觉/地点字符串确实进入终选prompt。
2. 一条东京环境需求从导出流到collector mock调用，并记录query-hit，满足后不继续请求。
3. outfit_only合同不能被参考分析扩大成环境或风格。
4. 远程成功、客户端未知、列表暂不可见、跨日恢复仍只保留一个任务。
5. 复用D1底图重排后不压脸，内页确实解释配色关系。

不建议再次加预设、扩量抓图或换模型。当前最需要的是检查输入是否到达真正的消费者，避免只新增参数、字段和孤立单元测试。
