# 图文素材消费：下一轮修复执行方案

日期：2026-09-17。基线：`7b878c1`。输入：`OPV_CONSUMPTION_FOLLOWUP_REVIEW_20260917.md`。本文是开发任务，不代表已实施。

## 1. 本轮目标与边界

完成三个结果：自动供稿失败可恢复且不突破额度；参考内容真正按账号需求被消费；国家/城市关键词进入独立采集，并按实际需求判断缺口。

保留已完成的零参考图生成、人物包、商品生成、渐变和封面层级修复。既有 TH、MX、围巾与手工生产入口继续运行。不重做后台、不新增运营 JSON、不全库重新分析、不扩大模型调用轮数。本轮不实现新的自动发布授权；它在本轮通过后单独接入，不能提前宣称无人值守发布已完成。

分 A/B/C/D 四批提交，逐批验收。开发前检查当前未提交改动，不覆盖其他模型正在修改的原创视频文件。只读/离线测试先行；真实采集、生图、发布使用明确限定的现有授权范围，不因执行开发任务自动开启全矩阵任务。

## 2. 批次 A：修复旧入口、恢复、预算和库存

### A1. 旧采集入库函数恢复并共享

文件：`labs/xhs-material-lab/scripts/search_to_library.py`、`collect_by_demand.py`。

- `import_feeds` 使用参数 theme/limit，删除函数内的 args 引用。
- 新旧入口真正调用同一入库函数，不保留两份相同逻辑；query_hits 由携带查询上下文的采集入口记录。
- 保持旧 CLI 参数、笔记 ID 去重、跳过视频、计数返回值兼容。

验收：用临时 SQLite 和本地 JSON 跑旧 CLI 与新入口；空结果不报错，重复笔记不新增，跨查询命中仍可记录来源。无需访问小红书。

### A2. 建行与合同绑定故障可恢复

文件：OPV `services/auto_photo_supply.py`、`external_supply_contract.py`、`material_analysis.py`、`feishu_workflow.py`。

执行顺序调整：

1. 先对账已有未完成合同和外部任务，再计算当日额度/库存。
2. 新行来源标记保留兼容前缀 `auto_supply|日期|账号`，追加稳定 slot/contract 标识；无需增加运营字段。
3. 冻结输入先持久化，后创建外部行；绑定失败或响应未知标记为待对账，不重选参考。
4. 下轮按唯一标记找到行后补绑合同、完成 slot；有多个匹配时停止该名额并记录异常，不自动删除或再创建。
5. 外部写入前复核租约，长调用期间续租；complete_slot 校验 owner。丢失所有权的 worker 不能继续提交后续副作用。
6. 响应未知且暂时查不到行时保留待对账，不把一次查无结果当成“必然未创建”。有服务端幂等键则沿用；没有时不能声称实现跨系统严格 exactly-once。

现有来源标记只含账号日期的遗留任务：仅在唯一可确定关联时自动补绑；有歧义保留并报告，不猜 slot。关闭自动模式时仍可做只读/本地对账，但不新增生产或发布。

恢复读取完整冻结需求，包括明确为空的商品/目的地/温度；不能因空值改用新账号默认。商品或参考版本变更需新 revision/合同版本，不能重试时覆盖原 intent。合同读取异常按错误处理，不按“没有合同”重建。

验收必须覆盖：建行成功但 attach_record 失败；远程成功但客户端超时；正常重复运行；两 worker 竞争；旧 owner 失去租约；账号配置变化后恢复。预期同名额最多一个可执行任务，冻结输入不变，无合同行能补绑。

### A3. 逐调用额度预留

文件：`services/material_analysis.py`、`material_adapter.py`、`auto_photo_supply.py`，以及自动任务实际付费调用入口。

- 复用现有调用台账，必要时增加 attempt_id、state、budget_day 等列；在一个短事务中检查并预留名额，不能只刷新内存计数。
- 每次模型尝试前预留；成功、已发起后失败、结果未知均占用次数；只有确认尚未发起的取消可释放预留。
- 成功后写真实 usage。接口未返回时写未知，不把缺失当成已知零；模型异常也写结果。
- 分开记录素材分析输入图片数、文字终选调用、生图尝试数。现有 IMAGE_CAP 继续表示分析输入图额度，不能拿来当生图上限。
- 日期按配置时区计算，明确日窗口起止；多 worker 共享同一账本原子限制。缓存命中不新增模型尝试。
- 本轮最低要求分析和终选限额可靠；如补生图额度，只接自动供稿任务已有生图入口，人工旧任务不受新默认值影响。尚未覆盖的成本阶段必须在交付说明列清。

验收：cap=1、两 slot 只能发起一次；两 worker 争最后一个额度也只能一次；失败后不能无限重试；跨日窗口正确。不得通过把 cap 调高规避失败测试。

### A4. 统一库存口径

文件：`auto_photo_supply.py` 及现有任务/成片查询接口。

库存包括同目标账号生成中、合格待发、排队中的有效内容，包含往日和手工任务；按任务子项/有效 revision 去重，不按每条飞书行固定加一。

- 优先读已有机器状态、子任务/成片状态；同一内容处于生成表和发布队列不能算两次。
- 尚未展开的多篇任务按剩余生产名额预留库存；展开后用真实子项替代预留。四张图片是一篇内容，不是四篇库存。
- 若暂需飞书兼容，规范化“素材生成 1/4”等实际进度文本，读取生成篇数；部分已发布按剩余篇数算。无法可靠算出时标明未知、暂不增加自动任务，不能按零处理。
- 同账号的库存检查与名额预留置于现有账号/slot 并发保护内，避免两个进程各自看到一个空位都补货。

验收：一行素材生成 1/4、篇数1，加一行生成中、篇数3，库存为4；跨日不清零；部分发布扣减；废弃/终止失败不占可用库存。

## 3. 批次 B：让账号需求与素材用途真正控制生成

### B1. effective_brief 前置

文件：`services/auto_photo_supply.py`、`material_adapter.py`、`external_supply_contract.py`、`publish_account_profile.py`。

在筛候选前解析本篇有效需求，贯穿初筛、终选、内容计划和合同：市场语言、账号定位、内容策略、明确主题/默认主题、商品及变体、视觉预设、目的地、明确温度、字段来源。

- 本篇明确字段覆盖相应账号默认；商品使用方式区分继承、本篇不指定、本篇指定。
- 定位优先：默认主题可作为方向；没有默认主题时在账号定位内提炼，不报缺主题错误。
- 参考优先：没有本篇明确主题时，从适合账号定位的素材提炼；账号默认主题只是偏好。要持续强主题使用已有定位优先选项。
- 主题未提供温度时不自动发明 15–22°C。明确选择的温度预设可以继续沿用其公开含义，并记录来源。

### B2. 内容选题与执行结构分开

文件：`auto_photo_supply.py`、`photo_content_planner.py`、`feishu_workflow.py`、相关文案生成/主题校验服务。

- `source_topic` 保留参考原题；`topic_statement` 必须是适配账号、商品后的本篇主张，不能直接复制“42套”或参考商品颜色。
- `recipe_id` 决定如何生产，`visual_preset` 决定背景呈现；两者不应覆盖 topic_statement。
- 删除自动供稿中“其他情况都填凉爽旅行”的分支。
- 优先复用已有非旅行四页结构；如当前四页生成入口强依赖旅行主题，只新增一个内部通用结构适配，不新增大量飞书主题选项。
- 通用结构使用页面计划，不注入机场/购物/咖啡/夜景、目的地、温度或投票 CTA；旅行明确主题才使用原旅行约定。
- 现有旅行手工任务按旧合同继续；新自动任务由新版消费合同显式选择路径。

例：灰色配色参考→“灰色穿搭如何做出层次”，每页解释实际配色；指定浅蓝外套→围绕浅蓝外套组织搭配，不能把深灰参考外套写成本篇商品。全链文案、中文回写与图像规划使用同一最终选题。

### B3. 用途约束统一，保留合理松绑

文件：`material_adapter.py`、`photo_reference_context.py`、实际参考分析/生图接入点。

- 删除终选 prompt 的自拍/poor 一律禁止规则，改为摄影质量影响视觉采用。
- 程序校验所选用途与 analysis 的可用用途一致；非法枚举或不相容选择不能默认 overall。
- overall 校验实际采用的维度，不要求 outfit、visual、narrative 三者全部可用，也不默认三者全部采用。
- outfit_only 可以只传搭配摘要；没有细节页不阻断。narrative_only 外部图清单必须为空，自己的商品/人物图独立保留。
- 将实际用途写入现有 reference_uses；不能一律自动补成 OUTFIT＋ENVIRONMENT＋VISUAL_STYLE。只借穿搭不继承来源人物、卡通装饰或地点。
- 输出不相容时先确定性丢弃未被允许的无关维度；若剩余内容不足以满足本篇需求，返回明确缺口，不偷偷变更主题/商品、不增加循环模型修复。
- 冻结合同指纹覆盖实际 brief、最终选题、用途、所选页、商品/人物锚点、视觉预设及计划版本；未知字段和兼容旧合同的处理要显式。

验收：视觉不可用的自拍能借搭配，但不能 visual_only；只借搭配的城市图不改变纯色背景；无参考图仍生产；自由主题不出现自动温度；指定商品颜色与本篇主张一致。

## 4. 批次 C：目的地关键词与真实素材缺口

### C1. 缺口保留最小需求

文件：`material_analysis.py` 现有 gap 台账、`auto_photo_supply.py`、`scripts/export_material_gaps.py`。

在现有 gap 增加程序维护的 demand JSON（或同等现有 JSON 容器），包含稳定需求键、主题方向、所需用途/结构、商品形态、目的地及其使用方式、简短原因、关联账号。相同需求合并；保留关联账号，不为每账号重复抓。

目的地需区分：用于搜索灵感、用于环境采用约束。选择东京旅行主题可以用“东京穿搭”搜索，即使只借穿搭；但候选筛选不因此强制拍摄地必须东京。

### C2. 可用库存按需求计算

复用消费侧匹配函数计算该需求的可用候选，而非“这个采集分类任意用途可用”。配置 purposes 明确是可替代用途还是需要同时具备，不把任意一个 flag=true 当满足全部。

一衣多穿的优选库存区分真实同件多搭和仅可借单套搭配的候选；若允许后者由规划重建，应显示为可适配，不能把其原始结构标成同件多搭。

查询族归属使用 family_id/query_hits；label 仅展示。历史 notes.theme 可做兼容映射，补入“旅行穿脱/层次”等别名；语义资格仍由分析和需求决定。计数按 note_id/有效版本去重。

已知缺口只能在重新检查相同需求确有候选后标为已补齐，不能因通用旅游冬装数量达到8就清掉东京环境缺口。

### C3. 自动组合目的地搜索词

文件：`labs/xhs-material-lab/config/query_families.json`、`scripts/collect_by_demand.py`、OPV `export_material_gaps.py`。

从现有账号/任务目的地与缺口生成少量查询，示例：

| 需求 | 定向查询示例 | 同时保留的通用查询 |
|---|---|---|
| 东京旅行层次 | 东京旅行叠穿、日本旅行外套穿脱 | 旅行方便穿脱搭配 |
| 首尔短外套 | 首尔短外套搭配、韩国旅行短外套 | 短外套不同下装 |
| 上海配色拍照 | 上海秋天穿搭配色（仅明确秋天时） | 灰色穿搭层次 |
| 指定浅蓝外套 | 东京浅蓝外套搭配 | 浅蓝短外套一衣多穿 |

国家/城市/季节/商品词均从明确字段或实际商品事实取，不补未知季节或温度，不用产品编码搜。无需建立热门城市排名或接实时天气。

每个需求先用小批查询，例如2条定向＋1条通用，程序配置可调。无目的地时只用通用查询；不做所有国家×主题×商品组合穷举。命中词保存为检索来源，不作为图片拍摄地证明。

环境借鉴关注可见地标冲突和可适配性，不要求每张背景都有明确城市证明；普通街景可注明地点未知。异地明显地标不能直接借作东京环境，服装搭配仍可使用。

### C4. 有限补分析与缓存

文件：`material_analysis.py`、`material_source.py`、`scripts/run_material_analysis.py`、`run_auto_photo_supply.py`。

- 0 候选时先进入一次有限补分析，再判断缺口，不能提前 return。补分析接正式入口，复用已有 Doubao。
- 支持传候选 note_ids/需求范围，优先当前缺口相关材料，不按全库入库顺序不断扫描。
- 若补入标题/正文或用途信息，缓存指纹包含实际使用的文本摘要与分析版本；判断当前指纹是否已分析，不能只看 note_id。
- 已有缓存继续使用，缺必要字段时仅对本轮少量候选补分析。取消为本轮验收全库 force 重析。
- 零候选、预算不足或采集失败只记录状态，待下一轮恢复，不要求人工逐篇审核，不同步等待浏览器。

验收：东京环境缺口在通用冬装库存充足时仍导出；纯穿搭允许首尔素材服务东京；20篇“旅行穿脱/层次”不会统计为0；国家城市进入实际查询预览；query_hits 保存来源；无关全库素材不触发分析。

## 5. 批次 D：回归与少量真实验收

### 离线必须项

除原165项相关测试外，新增针对上文缺陷的行为测试；删除/改写“无主题应推导凉爽旅行”等与目标相反的测试。

必须覆盖：旧CLI回归、建行成功绑定失败、响应未知对账、租约丢失、cap=1同轮多任务与双worker、失败计额度、多篇/跨日库存、用途不兼容、明确商品覆盖、自由主题、城市查询及需求库存。

实现重排后，运行受影响 OPV 测试，再运行该包完整离线回归；跨仓库不相关失败单列证据，不以“别人在改”直接忽略相关失败。静态测试通过与业务验收分开列。

### 真实小样

通过离线后，限定三条独立任务，使用真实终选，不固定返回素材：

1. 参考优先＋无主题＋配色素材：产出具体配色主题，非默认旅行/温度。
2. 定位优先＋本店浅蓝外套一衣多穿：商品一致、配套有实质变化、文案不残留参考商品颜色。
3. 明确东京或首尔旅行：查询预览含目的地，参考采用规则清楚，画面与文案无明显地点冲突。

至少一条使用纯色/室内背景，验证外景参考不会强制改变背景；零外部图路径以隔离测试为主，必要时复用已生成样片，不为每个边界都付费生图。

每条验收留存：effective_brief、原素材、采用计划、合同、实际输入图片、生成 provider/model/request_id（缺失明确写未知）、最终图片及中文。检查从“借鉴价值”到成片是否一致，不用 SHA 不同代替来源验证。

本轮样片不自动发布。完成报告应分“已通过/未实现/未实测”，不得只写全部完成；自动发布授权仍列下一阶段。

## 6. 迁移、发布与回滚

- 本地 SQLite 仅追加列/索引或必要的轻量调用预留记录，幂等迁移，开发先用临时库验证；历史合同/素材不删除。
- 新消费语义使用明确合同/策略版本，旧任务继续原语义，正在执行的任务不自动替换输入。
- 修复旧入口/预算/恢复可独立提交；新语义和搜索按现有自动账号范围小量启用，不能全账号默认打开。
- 回滚保留新增数据与冻结合同，通过关闭自动供稿或切回旧策略停止新任务；不能将新版未完成合同静默交给旧代码执行。
- 开发交付包含各批提交、测试命令与结果、迁移说明、三条真实验收记录和剩余项。无需新增运营配置表或生产逐篇审批。

---

# 附录：开发经理落点说明（2026-09-17，基线 7b878c1 核验）

> 本附录非方案原文。接手前已按方案逐项核验代码现状，以下缺陷**已实锤**（精确位置），
> 既有可复用资产与取代关系也一并列出，避免重复建设或行为叠加。

## 附A. 已验证的真实缺陷（方案点名、现场确认）

1. **A1 `args` 引用泄漏**：`labs/xhs-material-lab/scripts/search_to_library.py` 的
   `import_feeds` 函数体内仍引用 `args.limit`（C 批次重构只改了函数头尾，循环体
   `feeds[: args.limit]` 未参数化）——旧 CLI 之外的调用方一跑就 NameError。
   `collect_by_demand.py` 自带同构副本未触发，故 C 批次实跑没暴露。
2. **C4 零候选提前 return**：`auto_photo_supply.py:487` `if not narrowed: → no_material
   直接返回`，有限补分析（analyzer.analyze_pending）只在 `0 < narrowed < 3` 时触发
   ——0 候选时根本走不到补分析。
3. **B3 终选 prompt 残留整篇禁令**：`material_adapter.py:326`
   「绝对不要选择镜面自拍/poor」仍在——v3 初筛已按用途化（附B-2 上轮已取代），
   但终选 prompt 没同步，两处口径矛盾。

## 附B. 与既有实现的取代关系（实现时显式替换）

| 方案条目 | 取代的现有行为 | 位置 |
|---|---|---|
| B2 删除「其他情况都填凉爽旅行」 | 上轮按附B-1 降级保留的主题推导兜底；对应测试 `test_reference_first_without_theme_derives_theme` 需改写为「配色素材→配色主张，不出现旅行/温度」 | auto_photo_supply `_produce_slot_plan` 主题推导块 |
| B3 reference_uses 不得默认三用途全补 | `photo_reference_vision._normalize_reference_uses` 空值默认 `["OUTFIT","ENVIRONMENT","VISUAL_STYLE"]`；`_travel_planning_images` 同款默认 | photo_reference_vision.py:103 附近 |
| A3 预留制预算 | 现为运行前一次性扫描（`daily_call_usage`），非逐调用原子预留 | auto_photo_supply `_run_account` 预算门 |
| A4 库存按子项/revision 去重 | 现 `INVENTORY_ACTIVE_STATES` 按飞书行计数；「素材生成 1/4」文本未规范化；4 图=1 篇未折算 | auto_photo_supply `_scan_task_rows` |
| A2 标记追加 slot/contract 标识 | 现标记 `auto_supply|日期|账号`（对账按此计数）；追加后 `_count_existing_auto_rows` 解析需兼容两种格式 | auto_photo_supply MARKER 相关 |

## 附C. 可直接复用的资产（勿重建）

- 租约三件套（acquire/renew/release + 迁移）、合同存储/恢复/supersede、
  `import_feeds` 双入口共享目标（修 args 后即达成 A1）、query_hits 表、
  需求清单导出骨架（export_material_gaps 的 build_demand 可扩 demand JSON）、
  effective_brief 已冻结进合同（B1 是把它**前置**到筛候选之前并贯穿，不是新建）。
- 测试基线 1506；D 批要求改写/删除的测试清单：上表 B2 一项 +
  `test_reference_first_writes_topic_requirement`（若断言含推导标注需同步）。

## 附D. 执行顺序建议

A1（10 分钟独立修）→ A2 → A3 → A4（各自独立提交）→ B1 → B2+B3（同批，测试改写一起）
→ C1-C4（依赖 A/B 的 demand JSON 与用途口径）→ D 离线回归 → 三条真实小样。
每批提交前跑 `uv run --python 3.12 --with pytest,pillow,requests,httpx,numpy,scipy,pymysql python -m pytest tests/ -q`。
