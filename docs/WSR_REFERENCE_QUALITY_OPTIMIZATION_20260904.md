# 假发复刻素材与质量优化交付 — 2026-09-04

## 运行边界

已实施素材清单、图序纠偏、WSR首帧缓存、母版关键点检查和告警式成片检查代码。没有重新生成视频、修改人工正文、改发布排期或提高巡检频率。新增飞书字段仅为“内容检查结果”“内容检查说明”，先dry-run确认再创建，重复dry-run确认字段已就绪。

用户随后明确授权仅在测试阶段检查，不纳入日常生成。已移除生产H3提单、回流与恢复路径的自动模型检查入口；旧contentReviewEnabled配置即使为true也不再触发检查。独立测试入口保留。

本轮4条既有视频已通过现有gpt-5.6-sol/high完成真实抽帧检查，每条只调用一次：3条有偏差、1条证据不足。该结果验证了测试检查工具，不代表优化后重新生成的视频效果；本轮没有重新生成视频。

## 已落地

### 素材清单 V2

- 统一有序 person_identity / product / composite_first_frame 角色。
- 编译阶段读取全部选中图片，不再只取两张人物图。
- 原图 original_sha256 与压缩/实际输入 derived_sha256 分开记录。
- manifest_id按原图内容、顺序及角色计算，不包含临时URL或token。
- 跨Base转存保留原图身份，运行合同绑定真实目标附件token和实际字节hash。
- 新提示词不写死“前两张、后两张”；旧已知图序句只修正执行副本，不修改原文。
- H3逐张解码及校验hash，提单前保存最终请求、图序、图片hash、上传文件ID及本地证据路径。
- 仅默认复用本地图片字节。远端mm_file有效期未验证，因此远端引用复用默认关闭。

### 首帧缓存

- 仅WSR分支修复ready早退：先比较当前正文和原图内容，再判断缓存。
- 换图、改正文失效；同图换token可复用；有冻结清单时同原图不同派生编码不改变原图身份。
- 未勾选首帧不触发；历史未知缓存不批量重生成。
- 已提交/排期保护保留，上传或回写失败复用已生成本地图片。
- 独立首帧入口按运行任务ID单条查询H3状态，不扫表；已提交、状态未知或读取失败时跳过，不生成、不改写为首帧失败。同步入口复用已有目标快照，避免重复查询。

### 母版关键点

- 复用现有母版分析/审查调用，提取3–5个关键点，不新增脚本模型阶段。
- 旧母版由既有机制字段派生关键点并明确标记legacy_derived，不冒充新模型审核结果。
- 带货与养号都检查明显遗漏或矛盾；不引入商品匹配分、声明审批或口播强制要求。
- 明确记录semantic_status=not_evaluated：静态检查不证明模型执行，也不证明平台不判重。
- 去掉精确0.3秒字面硬门槛；“不出现参考图中的文字/发色”等属性否定不再误判为删除人物或假发。

### 告警式成片检查

- 入口：skills/jimeng-video-generator/scripts/run_wsr_content_review.py。
- 默认是零模型dry-run，真实测试调用必须显式--apply-model；生产H3不调用该入口。
- 输入为冻结母版点、最终提示词、参考图及带时间标记的抽帧，不上传原MP4给模型。
- 全片低频抽帧，并围绕实际画面变化位置加密；时间标签近似，不声称精确0.3秒或9帧通过。
- 检查人物外观、商品外观、核心动作和因果顺序。
- 结果pass/fail/unknown与视频生成状态分开；fail需要有效时间证据，缺证据降为unknown。
- 缺少母版点只能对照最终提示词，不能宣称母版保持通过。
- 本地SQLite按视频/提示词/原图及派生图hash/母版点/策略版本缓存。模型调用前预留尝试，单检查最多两次；SDK不再额外重试。
- 超时、解析异常、准备失败只记检查异常，不重新生成视频、不改变发布闸门。
- 测试结果可按固定记录单独写回；回写失败只重试已有结果、不重调模型。生产回流不写检查结果，历史uploaded不自动批量重审。

## 验证

开发阶段独立回归通过：上游103项、同步103项、首帧44项、成片检查15项，合计265项Python测试；另有6组JavaScript测试全部通过。git diff --check通过。随后补充生产路径不调用模型的隔离测试并通过，完成4条真实模型测试。

现有四条视频进行了离线跨语言联测：Python清单均被JS正确消费，均为6张图，源正文保持不变，旧“前两张/后两张”语句在执行副本内消失，且只保留一份角色说明。

四条现有MP4均可读取，时长约15.08秒；每条6张参考图原图可解码，完成全片与转场密集抽帧准备。没有给这些视频回填伪造的“质量通过”。

## 本轮测试结果

| 样本 | 结果 | 主要观察 |
|---|---|---|
| S260724014590 / H1 / e62de | 有偏差 | 两掌仍分色；移开手掌后才换发；人物外观漂移；商品主要特征匹配 |
| S260724014590 / H2 / d0ab6 | 有偏差 | 两掌仍分色；缺完整遮镜；人物外观及假发刘海有差异 |
| S260724029604 / G2第10条 / c4bf3 | 无法确认 | 商品外观基本匹配；关键转场采样存在空档，不能据此断言动作完成或失败 |
| S260724014590 / G2第10条 / a8764 | 有偏差 | 两掌仍分色；未完整遮镜就出现新发型；商品主要特征匹配 |

证据不足沿用现有飞书选项“检查异常”，说明中明确是抽帧无法确认，不是视频生成失败。所有结果仅为测试观察，不改变生成/发布状态，不作为审批闸门。模型依据抽帧比较可见外观，不能据此认定人物真实身份；时间均近似。

本轮原始结果位于runtime/wig-chain-audit-20260904/*-review-result.json；固定四条测试回写入口为同目录write_test_review_results.py，只核验并写入两项检查字段，不调用模型。
