# 图文养号生产加固执行结果（2026-09-03）

## 本次执行边界

完成工程修复、本地故障测试、生产结构部署及真实素材只读核验。没有生成新的图片/视频，没有创建生产任务、排期或发布。未修改人物/穿搭原始库及历史审核记录，账户仍为 testing。

## 已落地

### 恢复与批次

- 新增 `opv_production_batch`：创建任何子任务前冻结预期数量与分配清单；中断恢复补齐原清单，不按已有子集宣告整行完成。
- 缺少 P4 等非锚点素材时允许进入明确返工入口，不再因构建完整预览失败而挡住修复。
- 成片技术质检失败可选择“重做成片”；其余图片无需整组重画。
- 返工新 revision 激活、目标状态及投影原子更新；保留旧版验收历史。
- 扫描使用内核文件锁，进程死亡后释放；批次有持久化租约/心跳，过期任务明确转需处理，不会把在途结果不明当成可安全重发。
- 飞书回写有持久化待回写内容，支持重放。租约丢失时停止后续副作用。
- 新建飞书批次统一走 V2 三阶段审核；历史 V1 不自动迁移。随机组合已纳入穿搭拆解模板。
- 人工审核、模型审核和助手审图明确区分；助手审图不能冒充真人验收。已验收终审支持幂等，不允许以普通追加失败记录偷偷撤销。

### 发布安全

- 冻结 task/revision/render/review、镜头选择、文件哈希及文案为 release 清单。
- 入队及上传前均校验；上传前最多 20 秒只读复验 RDS 当前验收状态，异常即停止。
- 提交占位与请求参数先落库；平台提交结果不明时只对账，保留占位，禁止盲重发。
- 返工读取实际主发布队列，防止已经排队的版本被正常返工入口覆盖。
- BGM 保持发布时选择，冻结已提交音乐参数；未把计划选曲冒充平台实际音乐验收。

### 素材调用、准入和轮换

- 复用原人物/穿搭/场景库及 RDS 商品图包，不另造一份素材真相。
- 新穿搭拆解预设取消固定卧室，限定在账户允许且与穿搭兼容的场景池内选择；咖啡约会等显式场景主题仍不跨场景乱换。
- 联合检查商品编码、品类，以及已明确的版型、气候和场景约束。人物池过滤不可读参考、市场不匹配和不满足账户门槛的素材。
- 读取近期最多 100 条任务使用记录：避免近期完整组合，兼顾批内均衡与历史低频素材；商品图包优先批内均衡、同等次数再选历史少用包。
- 批次开始冻结分配，恢复不因近期历史改变而重新选人物、图包或模板。
- 生成前检查实际冻结素材，不再只看账户默认人物。商品和人物引用有字节哈希；图包文件改变后须重新入库。
- testing 至少 1 张人物参考；active 默认至少 3 张，且人物/穿搭/场景必须 enabled。缺鞋履在 testing 提示、在 active 阻断。
- 无 detail 参考只支持穿搭/结构中景，禁止微距证明；无独立下装的连衣裙组合不进入当前三件式拆解模板。
- 缺少真实配套单品图会明确记录为锚点派生，不当作真实单品实拍凭证。
- 审核提供 `run_visual_qa.py --force-recheck`，可显式忽略单图缓存重新判图；普通重试仍复用有效阶段结果。

## 已部署和核验

- OPV 全套 343 项测试、主发布器全套 171 项测试通过；编译与 `git diff --check` 通过。覆盖缺图返工、成片重做、批次中断补齐、锁回收、回写重放、审核终态及发布结果不明等边界。
- RDS 008 已应用，003–007 校验通过后跳过；001/002 保留原安装器的历史 bundle 校验记录，没有重写历史。
- `check_upgrade_readiness.py` 已覆盖 007/008 表、恢复字段及迁移校验；实际返回 `ready_for_003_008=1`。这是 schema 检查，不是生产质量验收。
- 飞书仅补充“重做成片”审核选项，没有修改任务行；仍为 13 个字段。
- 只读确认实际主发布 SQLite 已有 `submission_context_json`。
- 飞书 dry-run：6 行、0 条待执行、0 个错误。

## 真实素材只读验收

命令：`python3 scripts/check_asset_rotation.py 1737141103233042426 --quantity 9`

使用真实账户、原素材 SQLite、RDS 图包及历史，但不创建任务或调用模型：

| 维度 | 规划结果 |
| --- | --- |
| 人物 | 3 人，各 3 次 |
| 场景 | 机场、咖啡店、卧室各 3 次 |
| 穿搭 | 4 套兼容模板，2/2/3/2；不强行为凑数量选择不兼容模板 |
| 图包 | 2 包，5/4；优先历史少用包 |
| 素材门槛 | testing 通过，保留人物参考不足、鞋履缺项、无细节图和单品派生的警告 |

需要按正式生产门槛检查时，追加 `--require-production`。该开关仅在内存中检查，不修改账户状态。

实际按 active 门槛核验 3 套，结果 `production_ready=false`；阻断项为人物参考数量、人物/穿搭/场景未 enabled、鞋履缺项，符合预期，未自动放宽门槛。

## 尚不能宣称正式生产验收完成

1. 真实人物库目前每人仍只有 1 张参考；部分模板缺鞋履，部分素材仍 testing。代码不能替代缺失的原始素材或人工选品确认，不能直接把账户改成 active。
2. 新链路尚需真实小批生成验收：审核人物比例与自然度、商品一致性、拆解单品与上身对应、四种首图布局及实际成片效果。本轮规划成功不等于成品质量成功。
3. 成品感知相似度自动去重未实现；现有使用记录和组合去重不能代替视觉查重。
4. 平台对账当前查前 50 条，查不到继续保留结果不明，可能需要人工核对；提交占位后、实际请求前崩溃也需谨慎人工解除。
5. RDS 与发布 SQLite 不是分布式事务。已用占位与提交前复验拦住正常操作，但不宣称跨系统绝对原子。
6. 真实发布和平台实际 BGM 回执仍需在明确授权后验证；未进行任何真实发布尝试。

关联报告：`PRODUCTION_READINESS_AUDIT_20260903.md`（原问题清单）、`PUBLISH_SAFETY_20260903.md`（发布模块说明）。
