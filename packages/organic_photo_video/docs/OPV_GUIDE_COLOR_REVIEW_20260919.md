# 攻略／配色教程最新实现复核

日期：2026-09-19。基线：`4a45e42..3ac18cf`；工作区检查时干净。

对照 `OPV_GUIDE_COLOR_IMPLEMENTATION_PLAN_20260919.md`，检查实际代码、现存内容计划和成片，并运行离线测试。没有修改生产代码、账号配置、正式任务，没有调用生成、抓取或发布接口。未审计同期无关的项链原创功能。

## 结论

Phase A 部分修复；Phase B–D 增加了主题、配置和辅助代码，但“讲解规划→逐页图片→逐页文案→排版”未闭环，不能按全阶段完成验收。当前不需要继续换模型、增加主题或加严质检，应先把已有设计接通。

## 已确认生效

- 账号填写商品编码可推导指定商品模式；“自动”主题在选材前归一为空。
- 取消无条件填入统一奶白风格／灵感表达，允许继承已有账号配置。
- 新增“旅行·穿搭攻略”“配色教程”主题及 PRACTICAL_GUIDE 提示，两个主要配方的支持列表已放行。
- 四宫格图片改为完整适配格子，减少裁切头脚；但文字位置未接通，见下文。

## 必须修复的发现

### F1 [P1] 新教程主题没有进入教程规划，真实攻略仍回落投票文案

证据：

- `services/photo_theme.py:104` 注册新主题，但没有现有旅行主题分支使用的 `travel_theme_type`，也没有替代的工作流入口。
- `services/feishu_workflow.py:1815` 仅对既有旅行主题／原生一衣多穿构建 `travel_topic`；新教程的 topic 为空。
- `services/photo_reference_vision.py:978` 使用 topic 中的 theme_key 判断教程，因而真实入口不触发。
- 真实记录 `recvvDsLJdNAIq`、`recvvDtJs32UnL` 的 `content_plans/<record>/plan.json`：顶层 theme_key 为 TRAVEL_STYLING_GUIDE，input_contract.travel_topic 为 `{}`，没有 narrative_plan。copy_source 为 template_fill；封面和 CTA 仍含“选择 A/B/C/D”，正文还带默认 15–22°C。

这解释了为什么选了攻略，产物仍然相似。配方支持列表放行只解决入口拒绝，没有完成执行分流。

修复：以有效主题生成明确的内容执行上下文，让旅行和通用两条既有生产路径都能识别教程；主题、地点需求、表达模式沿同一上下文传递。不要仅强塞旧旅行类型来冒充教程。新主题有东京目的地范围时，应先确定有效地点需求再选材，不能依赖素材描述中是否出现“旅行”。专项临时探针已复现指定东京范围仍得到空 destination。

### F2 [P1] 即使补通入口，当前教程提示会丢失公共合同，叙事结构也没有消费者

- `photo_reference_vision.py:983` 将完整 base_prompt 替换为短 guide_block，丢掉原生成 JSON schema、商品、旅行变量、固定背景等公共上下文；这不是仅替换投票规则。
- `photo_content_planner.py:484` 的 `_guide_narrative_plan` 及其新配置没有实际调用链。
- `photo_reference_vision.py:1995` 的 narrative_plan 是从第一篇旧 slide_texts 事后拼出来的；参考依据硬编码空串，也没有逐页画面证据。多套任务只取 posts[0]。
- 未发现 downstream 读取 narrative_plan 驱动生图或渲染；`photo_theme.py:494` 仍按既有五段文案契约接纳输入，不能直接把四页新文案塞进去假定兼容。

修复：保留公共提示与输出契约，只切换“投票／讲解”业务规则；在既有规划调用里生成每篇的问题、2–3条建议、页职责、画面依据与本地语言文案。每篇独立保存叙事结构，明确四张来源与四页成片的映射；文案归一、中文回写和渲染消费同一结构。旧主题继续走旧分支。

### F3 [P1] 小红书讲解依据既未完整冻结，也未可靠送达规划

- `auto_photo_supply.py:1037` 新增 reference_basis，但 `external_supply_contract.py:146` 持久化白名单没有它。临时数据库实际 persist_intent 后返回对象不含该字段。
- `auto_photo_supply.py:1271` 拼接的是来源笔记前四页，不是实际采用页；指定商品槽位过滤也没有覆盖新增页摘要，能重新注入外部商品描述。
- narrative_only 不上传图片，selected_pages 为空；按上传图片挑语义依据会导致此模式没有依据。
- `photo_reference_vision.py:2099` 只取 content_requirement 前 200 字。两份真实缓存探针中，新增讲解分别从第 274、252 字开始，均被截掉。商品要求也可能位于被截尾部。

修复：将语义采用页与生图上传图片页分开；采用方法、证据页、允许用途放进已有可持久化的 JSON 容器并覆盖指纹，再由规划端读取。只注入实际采用且完成商品适配的摘要。按字段控制长度，不能对整个需求盲截 200 字。无需新增运营 JSON 配置或重分析全库。

### F4 [P2] 四宫格底部留白已经出现，文字却仍压人物

- `photo_package.py:139` 预留底部约 20%；本轮新增 contain 适配。
- `photo_package.py:421` 把原模板直接交给文字渲染，没有为该封面设置底部区域。
- `photo_structured_layout.py:267` 缺省仍在顶部。
- 实看 `photo_packages/opv_task_20260919_57dc8b2bef38/opv_rev_20260919_c13afbce8cc4/final/01.jpg`：左上人物脸及上身被文字覆盖，底部大块空白。这里是参数未接通，不只是字体审美。

修复：仅对四宫格封面将文字绑定到已经预留的区域，沿用现有字体体系；内页和单图封面保持现有配置。先复用现存四张图重渲染验收，无需付费重生。教程版式另需实际消费页要点与解释；目前没有新的教程渲染闭环，不能把 contain 修复视为教程版式完成。

### F5 [P1] 账号配置和任务恢复仍有上一轮要求修复的断点

1. `skills/short-video-auto-publisher/app/scheduler.py:413` 无条件补 own_tasks_only，使完全空配置也成为非空 profile；`:587` 取首个非空值时，空渠道行仍可遮蔽另一渠道的完整主题、商品和供稿配置。现有空配置测试直接失败。应先决议有效内容来源，再补默认领取范围。
2. `auto_photo_supply.py:326` 旧同日对账循环再次调用 attach_record，未捕获异常。前一循环虽然已隔离，同一合同又在后一循环抛出；临时故障探针复现整批 GLOBAL_ERROR，后续账号未处理。合并恢复逻辑，每合同只恢复一次，失败限制在对应账号。

字段兼容亦未齐：账号读取支持“指定商品编码”，建字段仍使用“默认产品编码”。作为入口兼容小修一并统一，不需要运营维护两列。

## 样片与测试结论

- 已查看上述最新四宫格封面，以及 `opv_task_20260919_f459943c7f26/opv_rev_20260919_258b7f0fbbc8/final/01.jpg` 旅行成片。后者仍为“四个造型／四种场合、选择 A/B/C/D”的展示。单纯好看的人像不能作为教程已生效的证据。
- 最新普通通勤／打卡任务保留选择型内容本身不属于缺陷；问题是新攻略主题的实际内容计划也回落同一路径。本次没有拿普通主题样片冒充新主题验收。
- 图文相关 12 个测试文件：**287 passed，7 subtests passed**，两项 Pillow 弃用警告。
- 发布器 `tests.test_supply_policy_profile`：**5 passed、1 failed**，失败为 `test_empty_everything_still_empty`。
- 本轮 diff 没有新增测试；未发现覆盖两个新主题从真实入口到规划／封版的测试。现有单元测试通过不代表新增业务链路通过。
- 未运行全部项目测试，未重新付费生成，未验证线上发布。本次是代码、现存产物与离线探针的评审。

## 下一轮最小收口顺序

1. 修空账号配置抢占、对账异常隔离与字段别名；给真实合并入口和多账号恢复补回归。
2. 一次接通教程执行上下文、模型叙事输出、素材依据持久化和逐页消费者。保持既有账户、商品、人物、背景、语言合同，不新增生产入口或额外质检轮次。
3. 修四宫格底部排字，并完成一种通用讲解排版；先用旧图离线重排检查文字和区域，再生成新教程样片。

验收用两条全新任务：旅行攻略（东京＋指定商品）和配色教程（固定背景）。须检查最终模型输入、每篇叙事结构、图像合同、四页最终图、中文回写；多套任务确保每篇有自己的讲解。外部调用可在回归测试中替换为确定性响应，但必须经过真实工作流入口与消费者。另复用一条普通四选一验证旧路不变。

具体业务验收：攻略说清“为什么这样搭／怎么照做”，每页画面能支撑对应建议；配色不是四套不同颜色的展示，而是可操作的配色方法；指定商品不被参考素材替换；封面和内页不再出现无关投票兜底；任务未给温度时不凭默认值编造确定天气。无需为本轮再建设新后台、自动推荐或复杂评分。
