# 飞书图文养号生产入口落地记录

## 已落地

- 目标表：wiki `TR10wxEXHiCYIhk8clActVdenpc` / table `tblj3x846gU3rshB`。
- 运营字段共 7 个：产品编码、生产预设、执行、进度、预览/成片、审核、备注。
- 生产预设把国家、账号、Recipe、Theme、Persona、Look、Scene 和文字层打包，运营不逐项配置。
- 飞书记录 ID 是外部幂等命名空间；核心任务、内容包、图片版本、渲染版本和错误明细只保存在 RDS。
- 图片生成后先回写 5 张预览图；人工通过后才进行整组批准和本地视频渲染。
- 支持 P1-P5 单张重做及整组重做；任务切换了不兼容预设时阻断混组并提示新建一行。
- 视频只生成本地审核成片并回写附件，不进入 NeoBund、不发布。

## 当前产品资料边界

入口通过产品编码复用 RDS 中最近一次包含参考图的不可变产品快照。新产品若尚未有快照，记录进入“需处理”，备注提示先完成产品资料入库；不会用缺失参考图的任务盲目生成。

## 验收

- 飞书 Schema 安装首次创建成功，再次执行 `created=[] / renamed=[]`，具备幂等性。
- 空表 dry-run：扫描 5 行，eligible=0，没有误触发生成。
- 全量单元测试通过，测试不连接真实 RDS、不调用图片模型、不发布内容。
