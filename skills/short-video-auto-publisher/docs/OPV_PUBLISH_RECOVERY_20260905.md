# OPV 发布恢复，2026-09-05

范围：2026-09-04 图文批次 9 条，源工作台记录 recvufx3c1c27d。

此前 9 条已于 21:42 入队，但 22:13 发布前核验子进程失败；父进程吞掉具体原因，并将第一次失败标记为不可重试。9 个对应未来槽位也被取消。恢复前逐条核验冻结清单通过。

修复：核验子进程输出安全结构化错误，包括阶段、异常类型、固定错误分类和数据库错误码。数据库/网络临时故障与超时允许现有的最多三轮重试；真实版本或文件不符仍阻止上传。原始错误文本、凭据与完整 stderr 不写入日志。

真实恢复又遇到 S3 PUT SSL EOF。已证明 upload 调用失败发生在 commit 之前，并查询平台记录未找到相应任务。现在上传阶段错误明确标记 submission_not_sent，进入有限重试；提交阶段的不确定结果继续对账，不能自动重发。

本机已有 HTTP 代理通过连接与平台列表查询验证。配置 neobund_proxy_url 仅作用于 NeoBund API 和 S3 上传会话，不改变其他服务网络设置。配置保存在机器级发布配置文件，不纳入仓库。

首次恢复使用 scripts/recover_opv_20260904.py，原始状态保存于 output/opv-recovery-20260905-081623.json。后续恢复仅处理失败子集，已创建平台任务保持原 ID。正式补排均传明确 canonical-key，沿用账号时间槽与 BGM 配置。

验证覆盖核验分类、三轮重试上限、真实不匹配、上传前失败不提交、提交后结果不明的对账规则，以及可选代理的会话作用范围。
