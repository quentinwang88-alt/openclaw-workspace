#!/usr/bin/env python3
"""
养号视频复刻提示词。
"""

from typing import Dict


CONTENT_BRANCH_PRODUCT = "product_visible"
CONTENT_BRANCH_NON_PRODUCT = "non_product"


GLOBAL_CONTROL_PROMPT = """【全局总控约束｜养号模式】

当前任务属于“养号视频复刻模式”，不是“带货视频复刻模式”。

所有步骤都必须遵守以下规则：
1. 账号内容优先于商品表达
2. 不要主动写成强带货结构
3. 不要主动生成价格、促销、购买理由、下单暗示
4. 不要写成直播口播、电商说服文案、强种草文案
5. 优先保留真实感、生活感、主页适配度、人物状态、场景氛围
6. 如果素材原本商业感太强，应主动降商业感，只保留结构、状态、人设、场景中的可复刻部分
7. 最终成片应让人感觉“像真实人在发内容”，而不是“像在卖东西”

【表达载体模式约束】
1. 不是所有视频都必须配口播
2. 模型必须自行判断原视频更接近：
   - 口播型（口播 + 字幕）
   - 静默字幕型（BGM + 字幕）
3. 如果原视频主要依靠 BGM + 字幕成立，复刻视频应优先继续采用静默字幕型
4. 不要为了“讲清楚”而强行给静默字幕型视频增加口播
5. 如果原视频本身更适合口播型，复刻时可继续保留口播型
6. 表达载体的选择优先服从“素材成立方式”，而不是“信息说得更全”"""


OPENING_STRATEGY_BLOCK = """【开头策略模块】
1. 模型内部只允许在以下 5 种中判断与选择：
   - 先抛矛盾
   - 先给变化
   - 先讲错误认知
   - 先亮态度
   - 先给一个小结论
2. 养号模式默认优先级：
   先亮态度 > 先讲错误认知 > 先给一个小结论 > 先抛矛盾 > 先给变化
3. 不允许输出自定义策略名，例如“视觉吸引开头”“轻打招呼”“情绪进入”
4. 开头不能写成直播带货钩子
5. 开头不能出现价格、促销、下单暗示
6. “先给变化”不能写成夸张效果承诺
7. “先抛矛盾”不能写成强反差销售句式
8. “先讲错误认知”必须写成个人经验修正，不能写成教育用户
9. “先给一个小结论”必须是小范围、场景化、主观化结论，不能写成绝对判断
10. “先亮态度”不能太空，必须尽快落到具体场景、状态或细节"""


OPENING_ENHANCEMENT_BLOCK = """【开头强化模块】
1. 开头不能只“类型正确”，还必须“具体、有画面、有口语感”
2. 开头至少满足以下 3 项：
   - 带有具体场景
   - 带有具体感受
   - 带有轻微反差或轻微新鲜点
   - 带有真实口语颗粒感
   - 能和首镜头动作直接咬合
3. 以下表达默认视为弱开头，除非后面立刻补具体场景、具体误判或具体细节，否则不要使用：
   - 最近很喜欢这种……
   - 这种真的很适合……
   - 这件很好搭……
   - 我最近常穿这种……
   - 这种感觉很舒服……
4. Step3 内部先生成 3 个同策略不同角度的开头候选，再内部筛选唯一版本：
   - 候选A：更偏场景进入
   - 候选B：更偏个人感受
   - 候选C：更偏轻微反差 / 新鲜点
5. 内部筛选标准：
   - 最像真实人在说话
   - 最不像广告钩子
   - 最能和首镜头动作结合
   - 最能支撑后续 3–7 秒展开
   - 最不空泛"""


DELIVERY_MODE_BLOCK = """【表达载体模式模块】
1. 模型内部只允许判断两种模式：
   - 口播型（口播 + 字幕）
   - 静默字幕型（BGM + 字幕）
2. 不要求用户在表格里填写表达载体模式
3. Step1 到 Step4 都要自行判断并保持一致
4. 如果最终采用口播型：
   - 输出口播脚本
   - 输出中文字幕对照版
   - 输出字幕建议
5. 如果最终采用静默字幕型：
   - 输出字幕脚本
   - 输出中文字幕对照版
   - 输出 BGM / 氛围建议
   - 不额外生成口播内容"""


STEP1_ANALYSIS_PROMPT_PRODUCT = """你是一个“养号视频复刻拆解助手（商品展示型分支）”。

任务：
请对素材做结构化拆解。当前任务的【内容分支】已经由表格锁定为：商品展示型。

说明：
1. 你必须按照“商品展示型”分支输出
2. 不允许自行切换到“非商品展示型”
3. 如果你判断当前分支可能不太合理，只能在“内容分支复核提示”里提醒，不允许修改路由
4. 保持养号模式，不要把拆解写成带货分析
5. 不要假设素材来源平台，也不要依赖外部填写开头方式或表达载体模式

{opening_block}

{delivery_block}

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 商品类型：{{product_type}}
- 复刻模式：{{replicate_mode}}

【请按以下格式输出】

一、基础判断
- 内容分支复核提示：
- 是否适合复刻：适合 / 一般 / 不适合
- 适合复刻的原因：
- 不适合的风险点：
- 推荐复刻模式：保守复刻 / 轻本地化复刻 / 只取状态不取结构

二、原视频核心逻辑提炼
- 这条视频真正吸引人的地方：
- 这条视频的核心表达主题：
- 这条视频最值得保留的自然吸引逻辑：
- 哪个结构最值得保留：
- 哪些表达需要本地化改写：

三、产品锚点卡
- 产品类型：
- 核心版型：
- 核心材质 / 质感：
- 核心颜色 / 视觉特征：
- 必须被镜头看见的细节：
- 商品自然露出方式：
- 允许调整的部分：
- 禁止改动项：

四、人物锚点卡
- 性别与年龄感：
- 人物外形气质：
- 长相强度：
- 发型 / 妆感方向：
- 情绪状态：
- 人物身份感：
- 允许调整的部分：
- 禁止改动项：

五、场景锚点卡
- 主场景：
- 场景类型：
- 必须保留的场景元素：
- 可替换的同类场景：
- 不允许替换成的场景：
- 场景氛围关键词：

六、镜头与15秒结构提炼
- 最适合前3秒保留的内容：
- 最适合中段承接的内容：
- 最适合商品自然露出的内容：
- 最适合轻收尾的内容：
- 推荐镜头数：4 / 5 / 6
- 推荐节奏：快 / 中 / 慢

七、开头方式识别
- 原视频当前使用的开头方式：
- 为什么这个开头成立：
- 这个开头更偏：
  - 真实分享感
  - 轻表达
  - 视觉吸引
  - 问题切入
  - 轻结论切入
- 是否适合继续用于养号复刻：适合 / 一般 / 不适合
- 如果继续使用，最需要避免的广告化风险：
- 如果不适合原样继承，建议替换成哪一种开头方式：
- 建议优先级：
  - 第一推荐：
  - 第二推荐：

八、表达载体识别
- 原视频当前表达载体模式：
- 这个模式为什么成立：
- 原视频主要依赖什么推动观看：
  - 人物说话感
  - 字幕信息推进
  - 画面状态推进
  - BGM氛围推进
  - 镜头节奏推进
- 是否适合在复刻中保持原模式：适合 / 一般 / 不适合
- 如果不适合原样继承，建议切换为哪种模式：
- 如果误判模式，最容易导致视频变成什么问题：

九、给下游模型的交接摘要
请输出一段可直接给 Step2 使用的简洁摘要，必须包含：
1. 核心表达主题
2. 产品锚点
3. 人物锚点
4. 场景锚点
5. 商品自然露出方式
6. 建议的开头方向
7. 建议的表达载体模式""".format(
    opening_block=OPENING_STRATEGY_BLOCK,
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP2_CARD_PROMPT_PRODUCT = """你是一个“养号视频复刻卡生成助手（商品展示型分支）”。

任务：
请根据 Step1 的拆解结果，生成一张压缩后的复刻卡。当前任务的【内容分支】已经锁定为：商品展示型。

要求：
1. 这不是成交卡，也不是卖点卡
2. 要保留商品自然露出方式，但不要把商品写成绝对主角
3. 开头方式和表达载体模式都由模型根据素材判断，不依赖表格额外字段
4. 复刻卡要服务于 Step3 写文案、Step4 做执行定稿

{opening_block}

{delivery_block}

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 商品类型：{{product_type}}
- 复刻模式：{{replicate_mode}}
- Step1 拆解结果：
{{analysis_result}}

【请按以下格式输出】

一、基础判断
- 是否适合复刻：
- 推荐复刻模式：
- 这条视频最值得复刻的原因（1句话）：

二、复刻核心
- 核心表达主题：
- 最值得保留的自然吸引逻辑：
- 最适合前3秒放大的内容：
- 最适合15秒里保留的商品自然露出点：
- 最适合轻收尾的内容：
- 推荐视频结构：

三、产品锚点摘要
- 产品类型：
- 核心版型：
- 核心材质 / 质感：
- 核心颜色 / 视觉特征：
- 必须被镜头看见的细节：
- 商品自然露出方式：
- 禁止改动的产品信息：

四、人物锚点摘要
- 人物年龄感与身份感：
- 人物外形气质：
- 长相强度：
- 发型 / 妆感方向：
- 情绪状态：
- 禁止改动的人物信息：

五、场景锚点摘要
- 原视频主场景：
- 场景类型：
- 必须保留的场景元素：
- 可替换的同类场景：
- 禁止替换成的场景：
- 场景氛围关键词：

六、场景与穿搭执行规则
- 原视频主场景：
- 原视频主穿搭：
- 场景改动规则：
- 穿搭改动规则：
- 是否允许做本地场景替换：允许 / 不允许
- 是否允许做穿搭微调：允许 / 不允许

七、镜头执行摘要
- 必须保留的镜头类型（最多3个）：
- 推荐镜头数：4 / 5 / 6
- 推荐节奏：快 / 中 / 慢
- 最不该删掉的镜头内容：
- 可以压缩的内容：

八、允许调整范围
- 允许本地化调整的内容：
- 允许轻微改写的内容：
- 绝对不能发挥过大的部分：

九、开头策略摘要
- 推荐优先开头方式：
- 可接受备选开头方式：
- 不建议使用的开头方式：
- 推荐原因：
- 开头语气要求：
- 开头最需要避免的广告化风险：

十、表达载体建议
- 推荐优先表达载体模式：
- 可接受备选模式：
- 推荐原因：
- 如果采用口播型，最需要避免的风险：
- 如果采用静默字幕型，最需要避免的风险：
- 该视频更适合依靠什么成立：

十一、下游交接摘要
请输出给 Step3 的简洁交接，必须包含：
1. 核心表达主题
2. 产品硬锚点
3. 人物硬锚点
4. 场景硬锚点
5. 商品自然露出方式
6. 推荐开头方式
7. 推荐表达载体模式

十二、参考图交接摘要
请输出给参考图模型的简洁交接，必须包含：
1. 商品如何呈现
2. 人物应保持什么感觉
3. 场景不能改成什么
4. 整体调性是什么""".format(
    opening_block=OPENING_STRATEGY_BLOCK,
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP3_LOCALIZED_SCRIPT_PROMPT_PRODUCT = """你是一个“养号视频本地化编剧（商品展示型分支）”。

任务：
请根据复刻卡生成一条 15 秒左右的本地化养号复刻脚本。当前任务的【内容分支】已经锁定为：商品展示型。

职责边界：
1. Step3 只负责文案与口播 / 字幕生成
2. Step3 不负责最终执行定稿
3. 商品可以出现，但不能抢戏
4. 不要把内容写成商品讲解视频

{opening_block}

{opening_enhancement_block}

{delivery_block}

【额外约束】
1. 必须优先继承复刻卡中的产品锚点、人物锚点、场景锚点、商品自然露出方式
2. 不允许为了增强抓力而把开头写成广告钩子
3. 静默字幕型的 0–3 秒字幕不能只是 mood 标签、emoji 或空泛判断
4. 开头如果偏平，只允许在当前策略内做增强，不允许切换成更广告化的写法

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 商品类型：{{product_type}}
- 复刻模式：{{replicate_mode}}
- 复刻卡：
{{replicate_card}}

【请按以下格式输出】

一、复刻执行判断
- 当前更适合采用：
- 本次必须继承的产品锚点：
- 本次必须继承的人物锚点：
- 本次必须继承的场景锚点：
- 本次必须继承的商品自然露出方式：
- 本次允许调整的内容：
- 本次不允许调整的内容：

二、本地化改写策略
- 目标国家用户更适合的表达方式：
- 原视频中最需要本地化的部分：
- 本次更适合走哪种内容类型：
- 本次最需要避免的广告化问题：

三、开头策略选择
- 本次采用的开头方式：
- 为什么选这种而不是别的：
- 该开头最需要避免的广告化风险：
- 如果写偏，最容易偏成什么样：

四、开头候选内部筛选（仅用于选择，不要输出多个版本）
请先在内部生成 3 个同策略候选，再内部筛选唯一版本。
最终只输出：
- 最终筛选后的开头：

五、15秒本地化主复刻脚本（唯一版本）
请直接输出唯一版本，不要给多个平行版本。

输出：
1. 本条核心表达主题
2. 本次采用的开头方式
3. 本次采用的表达载体模式
4. 最终采用的前3秒开头
5. 开头对应的首镜头动作与画面内容

如果本次采用【口播型】，请输出：
- 15秒完整口播脚本（{{target_language}}）
- 中文字幕对照版
- 字幕建议

如果本次采用【静默字幕型】，请输出：
- 15秒字幕脚本（{{target_language}}）
- 中文字幕对照版
- BGM / 氛围建议
- 不额外生成口播内容

六、开头质量自检
- 这个开头最抓人的点是什么：
- 这个开头是否存在“太空 / 太稳 / 太像总结句”的问题：
- 这个开头是否存在“虽然不广告，但也不想继续看”的问题：
- 这个开头和首镜头是否形成合力：
- 如果存在问题，请直接修正并给出修正后版本：

七、文案真实感自检
- 这段文案最像真实分享的地方是什么：
- 哪一句如果太像广告，请直接改写：
- 是否有“讲得太满 / 太规整 / 太像在介绍商品”的问题：
- 是否有“商品存在感太弱，导致看不出内容和商品有关”的问题：
- 如果有，请直接修正并给出修正后版本：

八、表达载体模式自检
- 当前采用的表达载体模式是否与原视频成立方式一致：
- 如果采用口播型，是否有“说得太多、太像讲解”的问题：
- 如果采用静默字幕型，是否有“信息太弱、字幕太空、画面撑不住”的问题：
- 当前模式是否会让视频更像广告：
- 如果有问题，请优先在不切换模式的前提下修正；只有当前模式明显不成立时才允许切换备选模式并说明原因

九、给下游执行层的简洁交接
要求包含：
1. 核心表达主题
2. 商品最自然的出现方式
3. 最终主场景
4. 最终主穿搭
5. 文案整体语气
6. 最需要避免的广告化风险
7. 最终采用的开头方式
8. 最终采用的表达载体模式""".format(
    opening_block=OPENING_STRATEGY_BLOCK,
    opening_enhancement_block=OPENING_ENHANCEMENT_BLOCK,
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP4_FINAL_EXEC_PROMPT_PRODUCT = """你是一个“养号视频最终执行脚本生成助手（商品展示型分支）”。

任务：
请根据 Step3 结果输出唯一最终定稿版本。当前任务的【内容分支】已经锁定为：商品展示型。

职责边界：
1. Step4 只负责执行定稿，不重新改写文案方向
2. 必须继承 Step3 已确认的表达载体模式
3. 如果是静默字幕型，不要新增口播
4. 只做最小必要收束，不要重新分析 Step3 已确认的内容
5. 商品展示型可以保留商品自然露出相关执行提醒，但不要写成商品讲解片

{delivery_block}

【执行层硬约束】
1. 必须输出一个唯一最终版本，不给备选
2. 最终主场景只能有 1 个唯一场景，不能出现“/”“或”“均可”“可替换为”“同类场景”等表达
3. 最终主穿搭只能有 1 个唯一穿搭版本，不能保留并列选项
4. 最终固定分镜里的场景与穿搭，必须和“最终固定设定”完全一致
5. 不要长篇解释为什么这样定稿
6. 不要重复 Step3 已完成的真实性检查、广告化解释、主题复盘
8. 所有回答都要“短”，优先短语而不是长句
9. 除“最终固定分镜”外，其余每个条目尽量控制在 1 句内
10. 不要补充题目里没要求的分析、总结、复盘、备注
11. 人物、穿搭、场景这三类执行信息必须保留，不能因为压缩而省略
12. 不再单独输出“最终可运行脚本”“最终开头固定”“最终开头张力检查”“AI视频生成简版执行提示词”

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 商品类型：{{product_type}}
- Step3 结果：
{{localized_script}}

【请按以下格式输出】

一、最终固定设定
请直接锁定唯一版本，不要提供备选：
- 核心表达主题：
- 内容类型：
- 最终主场景：
- 最终主穿搭：
- 最终人物状态：
- 最终表达载体模式：
- 最终视频节奏：
- 最终镜头数：
- 最终画面气质关键词：

要求：
- 只写必要锁定项，不解释原因
- 每项尽量控制在短语或一句短句内
- 人物、穿搭、场景信息必须具体，不能写得过泛

二、最终固定分镜（唯一版本）
这是 Step4 的核心输出，也是唯一详细输出。

请按镜头顺序输出 4–6 个固定镜头。每个镜头都必须包含：
- 镜头编号
- 时长
- 场景
- 穿搭
- 镜头内容
- 镜头目的
- 最终字幕/旁白
- 中文对照
- 执行提醒

补充要求：
1. 如果最终采用【口播型】：
   - “最终字幕/旁白”里输出口播/字幕内容
2. 如果最终采用【静默字幕型】：
   - “最终字幕/旁白”里输出字幕内容
   - 在“执行提醒”里补充该镜头所需的 BGM/氛围说明
3. 商品展示型可以在“执行提醒”里补充商品自然露出要求，但不要写成商品讲解片
4. 分镜必须足够详细，能直接承担执行用途
5. 不再额外输出按时间段拆分的脚本

三、产品锚点锁定
请用最短必要说明输出：
- 产品硬锚点：
- 本条唯一主表达：
- 必须被镜头看见的细节：
- 禁止丢失的产品信息：

要求：
- 只写产品层必须锁定的信息
- 不展开分析，不写成长篇检查

四、产品锚点镜头映射
请按最终固定分镜填写：
- 哪个镜头负责展示领型：
- 哪个镜头负责展示腰部细节：
- 哪个镜头负责展示袖口细节：
- 哪个镜头负责展示面料感：
- 哪个镜头负责承接“不用塞衣角也好看”：
- 哪个镜头负责承接“不透 / 好打理”：

要求：
- 如果当前产品不适用某项，请明确写“无此锚点”或“当前产品不适用”
- 只做镜头编号映射，不展开解释

五、负面限制词
要求：
- 避免强广告、强带货、价格促销、商品讲解感过重
- 避免直播口播感、硬种草感、夸张效果承诺""".format(
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP1_ANALYSIS_PROMPT_NON_PRODUCT = """你是一个“养号视频复刻拆解助手（非商品展示型分支）”。

任务：
请对素材做结构化拆解。当前任务的【内容分支】已经由表格锁定为：非商品展示型。

说明：
1. 你必须按照“非商品展示型”分支输出
2. 不允许自行切换到“商品展示型”
3. 如果你判断当前分支可能不太合理，只能在“内容分支复核提示”里提醒，不允许修改路由
4. 拆解重点是内容成立方式、人物状态、场景氛围、字幕 / BGM 推进方式
5. 不要强制商品出现，不要输出产品锚点卡

{opening_block}

{delivery_block}

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 商品类型（如有可忽略）：{{product_type}}
- 复刻模式：{{replicate_mode}}

【请按以下格式输出】

一、基础判断
- 内容分支复核提示：
- 是否适合复刻：适合 / 一般 / 不适合
- 适合复刻的原因：
- 不适合的风险点：
- 推荐复刻模式：保守复刻 / 轻本地化复刻 / 只取状态不取结构

二、原视频核心逻辑提炼
- 这条视频真正吸引人的地方：
- 这条视频的核心表达主题：
- 这条视频最值得保留的自然吸引逻辑：
- 哪个结构最值得保留：
- 哪些表达需要本地化改写：

三、主题锚点卡
- 主题锚点：
- 核心表达对象：
- 最值得保留的内容成立方式：
- 必须保留的情绪落点：
- 可调整的部分：
- 禁止改动项：

四、人物锚点卡
- 性别与年龄感：
- 人物外形气质：
- 长相强度：
- 发型 / 妆感方向：
- 情绪状态：
- 人物身份感：
- 允许调整的部分：
- 禁止改动项：

五、场景锚点卡
- 主场景：
- 场景类型：
- 必须保留的场景元素：
- 可替换的同类场景：
- 不允许替换成的场景：
- 场景氛围关键词：

六、情绪 / 状态锚点卡
- 核心状态：
- 情绪推进方式：
- 观众主要会感知到什么：
- 最容易破坏真实感的点：

七、内容成立方式判断
- 当前内容更主要靠什么成立：
  - 人设成立
  - 状态成立
  - 氛围成立
  - 场景成立
  - 字幕 / BGM 成立
- 为什么这样判断：
- 后续复刻最需要保留的成立条件：

八、开头方式识别
- 原视频当前使用的开头方式：
- 为什么这个开头成立：
- 这个开头更偏：
  - 真实分享感
  - 轻表达
  - 视觉吸引
  - 问题切入
  - 轻结论切入
- 是否适合继续用于养号复刻：适合 / 一般 / 不适合
- 如果继续使用，最需要避免的广告化风险：
- 如果不适合原样继承，建议替换成哪一种开头方式：
- 建议优先级：
  - 第一推荐：
  - 第二推荐：

九、表达载体识别
- 原视频当前表达载体模式：
- 这个模式为什么成立：
- 原视频主要依赖什么推动观看：
  - 人物说话感
  - 字幕信息推进
  - 画面状态推进
  - BGM氛围推进
  - 镜头节奏推进
- 是否适合在复刻中保持原模式：适合 / 一般 / 不适合
- 如果不适合原样继承，建议切换为哪种模式：
- 如果误判模式，最容易导致视频变成什么问题：

十、给下游模型的交接摘要
请输出一段可直接给 Step2 使用的简洁摘要，必须包含：
1. 核心表达主题
2. 主题锚点
3. 人物锚点
4. 场景锚点
5. 情绪 / 状态锚点
6. 建议的开头方向
7. 建议的表达载体模式""".format(
    opening_block=OPENING_STRATEGY_BLOCK,
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP2_CARD_PROMPT_NON_PRODUCT = """你是一个“养号视频复刻卡生成助手（非商品展示型分支）”。

任务：
请根据 Step1 的拆解结果，生成一张压缩后的复刻卡。当前任务的【内容分支】已经锁定为：非商品展示型。

要求：
1. 复刻对象可能是生活状态、场景关系、人设感、审美气质、字幕推进方式或 BGM 氛围
2. 不强制商品出现
3. 开头方式与表达载体模式都由模型根据素材判断，不依赖表格额外字段
4. 复刻卡要服务于 Step3 写文案、Step4 做执行定稿

{opening_block}

{delivery_block}

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 复刻模式：{{replicate_mode}}
- Step1 拆解结果：
{{analysis_result}}

【请按以下格式输出】

一、基础判断
- 是否适合复刻：
- 推荐复刻模式：
- 这条视频最值得复刻的原因（1句话）：

二、复刻核心
- 核心表达主题：
- 最值得保留的自然吸引逻辑：
- 最适合前3秒放大的内容：
- 最适合中段承接的状态 / 氛围：
- 最适合轻收尾的内容：
- 推荐视频结构：

三、主题锚点摘要
- 主题锚点：
- 核心表达对象：
- 最需要保留的成立方式：
- 禁止跑偏的主题方向：

四、人物锚点摘要
- 人物年龄感与身份感：
- 人物外形气质：
- 长相强度：
- 发型 / 妆感方向：
- 情绪状态：
- 禁止改动的人物信息：

五、场景锚点摘要
- 原视频主场景：
- 场景类型：
- 必须保留的场景元素：
- 可替换的同类场景：
- 禁止替换成的场景：
- 场景氛围关键词：

六、状态 / 氛围执行规则
- 核心状态：
- 情绪推进方式：
- 字幕 / BGM / 镜头各自承担什么：
- 哪些元素可以微调：
- 哪些元素不能结构性改变：

七、镜头执行摘要
- 必须保留的镜头类型（最多3个）：
- 推荐镜头数：4 / 5 / 6
- 推荐节奏：快 / 中 / 慢
- 最不该删掉的镜头内容：
- 可以压缩的内容：

八、开头策略摘要
- 推荐优先开头方式：
- 可接受备选开头方式：
- 不建议使用的开头方式：
- 推荐原因：
- 开头语气要求：
- 开头最需要避免的广告化风险：

九、表达载体建议
- 推荐优先表达载体模式：
- 可接受备选模式：
- 推荐原因：
- 如果采用口播型，最需要避免的风险：
- 如果采用静默字幕型，最需要避免的风险：
- 该视频更适合依靠什么成立：

十、下游交接摘要
请输出给 Step3 的简洁交接，必须包含：
1. 核心表达主题
2. 主题硬锚点
3. 人物硬锚点
4. 场景硬锚点
5. 状态 / 氛围执行规则
6. 推荐开头方式
7. 推荐表达载体模式

十一、参考图交接摘要
请输出给参考图模型的简洁交接，必须包含：
1. 人物应保持什么感觉
2. 场景应该是什么质感
3. 哪些设定不能改掉
4. 整体调性是什么""".format(
    opening_block=OPENING_STRATEGY_BLOCK,
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP3_LOCALIZED_SCRIPT_PROMPT_NON_PRODUCT = """你是一个“养号视频本地化编剧（非商品展示型分支）”。

任务：
请根据复刻卡生成一条 15 秒左右的本地化养号复刻脚本。当前任务的【内容分支】已经锁定为：非商品展示型。

职责边界：
1. Step3 只负责文案与口播 / 字幕生成
2. Step3 不负责最终执行定稿
3. 不强制商品出现
4. 不要为了增强抓力而强行植入商品、卖点或购买理由
5. 优先保证真实感、状态感、人设感、场景感、主页适配度

{opening_block}

{opening_enhancement_block}

{delivery_block}

【额外约束】
1. 必须优先继承复刻卡中的主题锚点、人物锚点、场景锚点、状态 / 氛围执行规则
2. 不要把脚本写成广告文案
3. 静默字幕型的 0–3 秒字幕不能只是 mood 标签、emoji 或空泛判断

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 复刻模式：{{replicate_mode}}
- 复刻卡：
{{replicate_card}}

【请按以下格式输出】

一、复刻执行判断
- 当前更适合采用：
- 本次必须继承的主题锚点：
- 本次必须继承的人物锚点：
- 本次必须继承的场景锚点：
- 本次必须继承的状态 / 氛围规则：
- 本次允许调整的内容：
- 本次不允许调整的内容：

二、本地化改写策略
- 目标国家用户更适合的表达方式：
- 原视频中最需要本地化的部分：
- 本次更适合走哪种内容类型：
- 本次最需要避免的广告化问题：

三、开头策略选择
- 本次采用的开头方式：
- 为什么选这种而不是别的：
- 该开头最需要避免的广告化风险：
- 如果写偏，最容易偏成什么样：

四、开头候选内部筛选（仅用于选择，不要输出多个版本）
请先在内部生成 3 个同策略候选，再内部筛选唯一版本。
最终只输出：
- 最终筛选后的开头：

五、15秒本地化主复刻脚本（唯一版本）
输出：
1. 本条核心表达主题
2. 本次采用的开头方式
3. 本次采用的表达载体模式
4. 最终采用的前3秒开头
5. 开头对应的首镜头动作与画面内容

如果本次采用【口播型】，请输出：
- 15秒完整口播脚本（{{target_language}}）
- 中文字幕对照版
- 字幕建议

如果本次采用【静默字幕型】，请输出：
- 15秒字幕脚本（{{target_language}}）
- 中文字幕对照版
- BGM / 氛围建议
- 不额外生成口播内容

六、开头质量自检
- 这个开头最抓人的点是什么：
- 这个开头是否存在“太空 / 太稳 / 太像总结句”的问题：
- 这个开头是否存在“虽然不广告，但也不想继续看”的问题：
- 这个开头和首镜头是否形成合力：
- 如果存在问题，请直接修正并给出修正后版本：

七、文案真实感自检
- 这段文案最像真实分享的地方是什么：
- 哪一句如果太像广告，请直接改写：
- 是否有“讲得太满 / 太规整 / 太像在解释内容”的问题：
- 是否有“状态太弱，导致内容撑不住”的问题：
- 如果有，请直接修正并给出修正后版本：

八、表达载体模式自检
- 当前采用的表达载体模式是否与原视频成立方式一致：
- 如果采用口播型，是否有“说得太多、太像讲解”的问题：
- 如果采用静默字幕型，是否有“信息太弱、字幕太空、画面撑不住”的问题：
- 当前模式是否会让视频更像广告：
- 如果有问题，请优先在不切换模式的前提下修正；只有当前模式明显不成立时才允许切换备选模式并说明原因

九、给下游执行层的简洁交接
要求包含：
1. 核心表达主题
2. 主题最自然的落地方式
3. 最终主场景
4. 最终主人物状态
5. 文案整体语气
6. 最需要避免的广告化风险
7. 最终采用的开头方式
8. 最终采用的表达载体模式""".format(
    opening_block=OPENING_STRATEGY_BLOCK,
    opening_enhancement_block=OPENING_ENHANCEMENT_BLOCK,
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP4_FINAL_EXEC_PROMPT_NON_PRODUCT = """你是一个“养号视频最终执行脚本生成助手（非商品展示型分支）”。

任务：
请根据 Step3 结果输出唯一最终定稿版本。当前任务的【内容分支】已经锁定为：非商品展示型。

职责边界：
1. Step4 只负责执行定稿，不重新改写文案方向
2. 必须继承 Step3 已确认的表达载体模式
3. 如果是静默字幕型，不要新增口播
4. 不强制商品出现
5. 不要为了增强抓力而硬塞商品
6. 只做最小必要收束，不要重新分析 Step3 已确认的内容

{delivery_block}

【执行层硬约束】
1. 必须输出一个唯一最终版本，不给备选
2. 最终主场景只能有 1 个唯一场景，不能出现“/”“或”“均可”“可替换为”“同类场景”等表达
3. 最终主人物状态只能有 1 个唯一主状态，不要保留多个平行方向
4. 最终主穿搭如果有提及，也只能保留 1 个唯一版本
5. 主题不能跑偏，不能因为增强抓力而变得太像广告
6. 不要长篇解释为什么这样定稿
7. 不要重复 Step3 已完成的真实性检查、表达载体自检、主题复盘
8. 所有回答都要“短”，优先短语而不是长句
9. 除“最终固定分镜”外，其余每个条目尽量控制在 1 句内
10. 不要补充题目里没要求的分析、总结、复盘、备注
11. 人物、穿搭、场景这三类执行信息必须保留，不能因为压缩而省略
12. 不再单独输出“最终可运行脚本”“最终开头固定”“最终开头张力检查”“AI视频生成简版执行提示词”

【输入信息】
- 内容分支：{{content_branch_label}}
- 目标国家：{{target_country}}
- 目标语言：{{target_language}}
- 商品类型（如有可忽略）：{{product_type}}
- Step3 结果：
{{localized_script}}

【请按以下格式输出】

一、最终固定设定
请直接锁定唯一版本，不要提供备选：
- 核心表达主题：
- 内容类型：
- 最终主场景：
- 最终主人物状态：
- 最终主穿搭（如有）：
- 最终表达载体模式：
- 最终视频节奏：
- 最终镜头数：
- 最终画面气质关键词：

要求：
- 只写必要锁定项，不解释原因
- 每项尽量控制在短语或一句短句内
- 人物、穿搭、场景信息必须具体，不能写得过泛

二、最终固定分镜（唯一版本）
这是 Step4 的核心输出，也是唯一详细输出。

请按镜头顺序输出 4–6 个固定镜头。每个镜头都必须包含：
- 镜头编号
- 时长
- 场景
- 穿搭
- 镜头内容
- 镜头目的
- 最终字幕/旁白
- 中文对照
- 执行提醒

补充要求：
1. 如果最终采用【口播型】：
   - “最终字幕/旁白”里输出口播/字幕内容
2. 如果最终采用【静默字幕型】：
   - “最终字幕/旁白”里输出字幕内容
   - 在“执行提醒”里补充该镜头所需的 BGM/氛围说明
3. 非商品展示型不强制商品出现，不要在分镜里硬塞商品
4. 分镜必须足够详细，能直接承担执行用途
5. 不再额外输出按时间段拆分的脚本

三、负面限制词
要求：
- 避免硬塞商品、硬做卖点、强行种草、强解释
- 避免直播口播感、广告片感、说服型文案""".format(
    delivery_block=DELIVERY_MODE_BLOCK,
)


STEP1_ANALYSIS_PROMPT = STEP1_ANALYSIS_PROMPT_PRODUCT
STEP2_CARD_PROMPT = STEP2_CARD_PROMPT_PRODUCT
REMADE_SCRIPT_PROMPT = STEP3_LOCALIZED_SCRIPT_PROMPT_PRODUCT
FINAL_VIDEO_PROMPT = STEP4_FINAL_EXEC_PROMPT_PRODUCT


def _normalize(value: str, default: str = "未提供") -> str:
    value = (value or "").strip()
    return value or default


def _context_value(context: Dict[str, str], *keys: str) -> str:
    for key in keys:
        value = (context.get(key) or "").strip()
        if value:
            return value
    return ""


def _resolve_content_branch(context: Dict[str, str]) -> str:
    value = _context_value(context, "content_branch", "内容分支")
    if value in {CONTENT_BRANCH_PRODUCT, "商品展示型"}:
        return CONTENT_BRANCH_PRODUCT
    if value in {CONTENT_BRANCH_NON_PRODUCT, "非商品展示型"}:
        return CONTENT_BRANCH_NON_PRODUCT
    return CONTENT_BRANCH_PRODUCT


def _content_branch_label(branch: str) -> str:
    if branch == CONTENT_BRANCH_NON_PRODUCT:
        return "非商品展示型"
    return "商品展示型"


def _replace_common_vars(template: str, context: Dict[str, str]) -> str:
    branch = _resolve_content_branch(context)
    replacements = {
        "{{content_branch}}": branch,
        "{{content_branch_label}}": _content_branch_label(branch),
        "{{target_country}}": _normalize(_context_value(context, "target_country")),
        "{{target_language}}": _normalize(_context_value(context, "target_language")),
        "{{product_type}}": _normalize(_context_value(context, "product_type")),
        "{{replicate_mode}}": _normalize(_context_value(context, "replicate_mode", "remake_mode"), "轻本地化复刻"),
    }

    output = template
    for placeholder, value in replacements.items():
        output = output.replace(placeholder, value)
    return output


def _inject_global_control(prompt: str) -> str:
    return f"{GLOBAL_CONTROL_PROMPT}\n\n{prompt}"


def _branch_runtime_input(context: Dict[str, str]) -> str:
    branch = _resolve_content_branch(context)
    return (
        "【本次任务输入】\n"
        f"- 内容分支：{_content_branch_label(branch)}\n"
        f"- 目标国家：{_normalize(_context_value(context, 'target_country'))}\n"
        f"- 目标语言：{_normalize(_context_value(context, 'target_language'))}\n"
        f"- 商品类型：{_normalize(_context_value(context, 'product_type'))}\n"
        f"- 复刻模式：{_normalize(_context_value(context, 'replicate_mode', 'remake_mode'), '轻本地化复刻')}\n"
    )


def _select_templates(branch: str):
    if branch == CONTENT_BRANCH_NON_PRODUCT:
        return (
            STEP1_ANALYSIS_PROMPT_NON_PRODUCT,
            STEP2_CARD_PROMPT_NON_PRODUCT,
            STEP3_LOCALIZED_SCRIPT_PROMPT_NON_PRODUCT,
            STEP4_FINAL_EXEC_PROMPT_NON_PRODUCT,
        )
    return (
        STEP1_ANALYSIS_PROMPT_PRODUCT,
        STEP2_CARD_PROMPT_PRODUCT,
        STEP3_LOCALIZED_SCRIPT_PROMPT_PRODUCT,
        STEP4_FINAL_EXEC_PROMPT_PRODUCT,
    )


def build_script_breakdown_prompt(context: Dict[str, str]) -> str:
    branch = _resolve_content_branch(context)
    template, _, _, _ = _select_templates(branch)
    prompt = _inject_global_control(_replace_common_vars(template, context))
    return f"{prompt}\n\n{_branch_runtime_input(context)}请严格按照要求输出，不要省略任何一级标题。"


def build_remake_card_prompt(context: Dict[str, str], script_breakdown: str) -> str:
    branch = _resolve_content_branch(context)
    _, template, _, _ = _select_templates(branch)
    prompt = _inject_global_control(_replace_common_vars(template, context))
    return (
        f"{prompt}\n\n"
        f"{_branch_runtime_input(context)}"
        f"- Step1 拆解结果：\n{script_breakdown}\n"
        "请只输出这一张复刻卡，不要补充额外说明。"
    )


def build_remade_script_prompt(context: Dict[str, str], remake_card: str) -> str:
    branch = _resolve_content_branch(context)
    _, _, template, _ = _select_templates(branch)
    prompt = _inject_global_control(_replace_common_vars(template, context))
    return (
        f"{prompt}\n\n"
        f"{_branch_runtime_input(context)}"
        f"- 复刻卡：\n{remake_card}\n"
        "请只输出唯一版本，不要给备选。"
    )


def build_final_video_prompt(context: Dict[str, str], remade_script: str) -> str:
    branch = _resolve_content_branch(context)
    _, _, _, template = _select_templates(branch)
    prompt = _inject_global_control(_replace_common_vars(template, context))
    return (
        f"{prompt}\n\n"
        f"{_branch_runtime_input(context)}"
        f"- Step3 结果：\n{remade_script}\n"
        "请只输出最终定稿版，不要再给备选解释。"
    )
