# 长视频轻量松绑验收（2026-09-04）

## 本轮范围

仅修改代码及离线测试；没有生成新脚本、图片或视频，没有写飞书、生产数据库，也没有提交或推送代码。

1. 普通切镜允许动作进行中结束；不再要求每镜站定或填写结束姿态。连续桥接仍可记录真实边界状态，不把连续等同于静止。
2. 中央长口播取消固定四步中段与逐段衔接开口。输入删除逐镜观看任务和解释，只保留主情境、购买理由、批准卖点、中央话术资源及分段时间/地点背景。模型路由、TTS与一次修订机制保持原样。
3. 复用现有曝光和商品背景分离能力，由同一轻量函数按每段实际场景提供给视频提示词和首帧。沿用冻结穿搭，不引入旧首镜或全身取景要求。
4. 长视频商品颜色仅作为穿搭软排序；非同色不再从候选中排除。批内少用优先、来源层级和适配逻辑不变；不传颜色参数的15秒路径不变。

## 验证

- 原创/长视频综合离线回归：147/147通过，覆盖运营入口、replan、首帧、视觉合同、穿搭、口播和长视频编排。
- 中央口播离线回归：19/19通过（包含3项新增长视频提示词测试）。
- 修改范围的 git diff --check 通过。
- 只读加载批次 LFB_47849DBDF884BCCB7EDA 的两条已存脚本，在内存重新编译：四个片段均保留3个观看任务；曝光/背景分离提示非空，首帧与视频端曝光一致。未保存重编译产物。

## 生效与边界

- 已冻结任务的 resume 保留原输入；新建任务或 replan 使用更新后的来源/计划版本。
- 安装技能目录为工作区源码的符号链接，无需额外复制部署。
- 本轮验证证明数据传递与兼容性，不等于新生成脚本自然度或成片效果已得到验证。
- 未新增审查门槛、模型调用阶段、重试或人工确认节点；自拍/他拍承载冲突等未纳入本轮范围。
